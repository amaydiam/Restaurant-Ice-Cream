<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/toolbar_text_holder"
    android:layout_width="wrap_content"
    android:layout_height="?attr/actionBarSize"
    android:layout_marginEnd="@dimen/dist_large"
    android:layout_marginRight="@dimen/dist_large"
    android:gravity="center_vertical"
    android:orientation="vertical">

    <com.ad.restauranticecream.widget.RobotoBoldTextView
        android:id="@+id/toolbar_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:ellipsize="end"
        android:maxLines="1"
        android:textColor="@android:color/white"
        android:textSize="@dimen/text_xlarge"
        android:textStyle="bold"
        tools:text="toolbar_title" />

    <com.ad.restauranticecream.widget.RobotoRegularTextView
        android:id="@+id/toolbar_subtitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:ellipsize="end"
        android:maxLines="1"
        android:textColor="@android:color/white"
        android:textSize="@dimen/text_small"
        tools:text="toolbar_subtitle" />

</LinearLayout>